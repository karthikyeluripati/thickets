# Reproduction Specification — Neural Thickets §5.2 (Qwen2.5-VL-3B-Instruct + GQA)

Target: arXiv:2603.12228 ("Neural Thickets: Diverse Task Experts Are Dense Around Pretrained Weights",
Yulu Gan & Phillip Isola, ICML 2026 Spotlight), §5.2 and Appendix E.3.

Published result: Base **56.6%** → RandOpt (N=5000, K=50) **69.0%** on GQA.

**No upstream source code or paper text is copied into this document.** Each row describes behavior in
our own words and cites where it was verified: a `paper §/Appendix` reference, or an
`upstream_file:function_name` reference into `github.com/sunrainyg/RandOpt` (pinned commit
`536df0a308f3990b6270c991fbb96bd0b779a58e`, see `external/EXTERNAL_COMMIT.txt`). The upstream repo has
no declared license (`license: None` per the GitHub API), so its code is never vendored or transcribed
here or anywhere in this repo — it is only read, described, and invoked as an external subprocess.

## Specification table

| Item | Published / official value | Source | Status |
|---|---|---|---|
| Model checkpoint | `Qwen/Qwen2.5-VL-3B-Instruct` | paper §5.2 | Confirmed |
| Model revision | commit `66285546d2b821cf421d4f5eb2576359d3770cd3` (HF hub `main` as of this check) | fetched directly from HF hub `config.json`/index ETag, this session | Confirmed as of check date; **not** pinned by the paper or upstream repo — we are choosing to pin it ourselves for reproducibility |
| Model architecture / class | `Qwen2_5_VLForConditionalGeneration`, `tie_word_embeddings=true` (no separate `lm_head.weight` tensor; it is tied to `model.embed_tokens.weight`) | fetched `config.json` + `model.safetensors.index.json` from HF hub, this session | Confirmed |
| Dataset | GQA | paper §5.2 | Confirmed |
| Dataset source | HuggingFace `lmms-lab-encoder/GQA` (renamed from `lmms-lab/GQA` -- confirmed via HTTP redirect from the old name; `upstream data/README.md` still cites the pre-rename name), or an author-provided Google Drive "all-in-one" archive | `upstream data/README.md`; HF hub redirect, this session | Confirmed as *a* source; see next row for our own resolution of the exact subset |
| Dataset revision / exact subset | **Resolved by documented reproduction assumption** (not upstream-confirmed): test split = `testdev_balanced` config (12,578 questions over 398 unique images, verified by actually generating it this session); selection split = first `selection_set_size` rows of `train_balanced` (943,000 questions total, verified this session). Upstream never showed how its own `train.parquet`/`testdev.parquet` were produced -- these are the standard, commonly-cited GQA splits and the most plausible match for upstream's own file names ("testdev.parquet" most plausibly means testdev-balanced, the split GQA papers conventionally report one headline number against), not a confirmed match to upstream's actual (unpublished) prep step. | `src/neural_thickets_repro/prepare_gqa_data.py`, validated against the real dataset this session (see script docstring for exact HF configs used) | Resolved-by-assumption, clearly distinct from Confirmed |
| Selection split | referred to as "training set" by upstream, loaded from a `train.parquet`-shaped file | `upstream_file:data_handlers/gqa.py:GQAHandler.load_data`, `default_train_path` | Confirmed path/role; exact subset resolved-by-assumption above |
| Test split | referred to as "testdev", loaded from a `testdev.parquet`-shaped file | `upstream_file:data_handlers/gqa.py:GQAHandler.load_data`, `default_test_path` | Confirmed path/role; exact subset resolved-by-assumption above |
| Selection-set size | 200 (first 200 examples of the selection/training split) | paper Appendix E.3 ("first 200 samples from the training set as the RandOpt training set"), matches `upstream_file:randopt.py` CLI default `--train_samples 200` | Confirmed — paper and code agree |
| Test-set size | 12,578 (all of testdev_balanced, no subsampling) | `upstream_file:randopt.py` CLI default `--test_samples None`; exact count confirmed by generating the split this session | Confirmed count under the resolved-by-assumption split choice above |
| Prompt template | An instruction telling the model to look at the image, answer the given question, reason step by step, and place the final short answer inside a boxed/delimited span | `upstream_file:data_handlers/gqa.py:GQAHandler.load_data` (described, not transcribed) | Confirmed |
| Chat formatting | Applied via the tokenizer's chat template when the model name indicates an instruct/chat model (true for Qwen2.5-VL-3B-**Instruct**) | `upstream_file:randopt.py:main` (`format_prompt`) | Confirmed |
| Image message format | Multimodal chat message with an image content block followed by the text prompt block, in the format vLLM/Qwen2.5-VL expects | `upstream_file:data_handlers/gqa.py:GQAHandler.load_data` | Confirmed |
| Image preprocessing | Not explicitly configured upstream; relies on vLLM/Qwen2.5-VL's own default image handling (resize/max-pixels policy) | absence checked in `upstream_file:data_handlers/gqa.py`, `upstream_file:core/engine.py` | **UNRESOLVED** — no explicit resolution/max-pixels setting found anywhere upstream |
| Answer normalization / extraction | Boxed-answer extraction takes priority; falls back to pattern-based extraction from the response text; normalization lowercases, strips punctuation/articles, singularizes, and maps a fixed synonym table (e.g. sofa↔couch) before comparison; a final whole-word scan over the response is used as a last-resort match | `upstream_file:utils/reward_score/gqa.py` (`extract_boxed`, `extract_answer`, `compute_score`), `upstream_file:data_handlers/gqa.py` (`_normalize_answer`, `_singularize`, `_canonicalize`, `_match_answer`, `_whole_word_search`) — behavior described, not transcribed | Confirmed (behavior read and understood; exact regex/word-list content deliberately not copied into this repo — see `external/` for the authoritative source) |
| Baseline decoding | Greedy decoding (temperature 0), fixed generation seed, `max_tokens=256` for GQA | `upstream_file:randopt.py:main` (`SamplingParams`), `upstream_file:data_handlers/gqa.py` (`default_max_tokens`) | Confirmed |
| Perturbation target | Language-model component only | paper §5.2 ("we perturb the language model while keeping the visual encoder frozen") | Confirmed |
| Perturbation scope (exact) | Every named parameter is perturbed **except** ones whose name is prefixed `visual.` or `model.visual.` — i.e. token embeddings and any LM head weights (here tied to embeddings, so N/A as a separate tensor) **are** perturbed; only the vision tower is excluded, unless an explicit opt-in override is set | `upstream_file:utils/worker_extn.py:WorkerExtension._should_perturb` | Confirmed against upstream code, and cross-checked against the real checkpoint's actual tensor names (fetched from HF hub `model.safetensors.index.json` this session): 100% of vision-tower tensors are prefixed `visual.` (`visual.blocks.*`, `visual.merger.*`, `visual.patch_embed.*`), everything else is `model.embed_tokens.*` / `model.layers.*` / `model.norm` — the `visual.` rule cleanly separates them with no ambiguous tensor names |
| Vision encoder | Frozen (bitwise unchanged across candidate perturbations) | paper §5.2, `upstream_file:utils/worker_extn.py:_should_perturb` | Confirmed |
| Perturbation formula | θ' = θ + σε, ε ~ N(0, I), generated fresh per parameter tensor from a seeded generator, added in-place in the parameter's native dtype (no fp32 accumulation in this code path); restoration re-seeds identically and subtracts the same noise rather than restoring from a stored weight copy | paper §4 (formula); `upstream_file:utils/worker_extn.py:perturb_self_weights`/`restore_self_weights` (mechanism) | Confirmed |
| N (population size) | 5000 | paper §5.2 | Confirmed |
| K (ensemble size) | 50 | paper §5.2 | Confirmed |
| Candidate (seed, sigma) assignment | For a run of size N: N unique seeds drawn without replacement from a large integer range; one sigma per candidate drawn **with** replacement from the configured sigma set; both draws come from one global-seed-derived RNG stream | `upstream_file:randopt.py:run_sampling` | Confirmed as the *mechanism*; the exact sigma set used for this experiment is unresolved (next row) |
| Sigma / sigma set | **UNRESOLVED — no single confirmed value or set for this exact experiment.** Three non-equivalent candidates found: (1) the code's global CLI default set; (2) the value used in the upstream example launch scripts (for a different model/dataset, Olmo-3-7B/countdown, not Qwen-VL/GQA); (3) a single value `σ=0.005` mentioned in paper Appendix E.3, in a sentence about "landscape analysis" whose scope relative to the headline GQA result is not clearly stated in the fetched text | paper Appendix E.3; `upstream_file:randopt.py` CLI default; `upstream_file:scripts/local_run.sh` / `scripts/single_node.sh` | **UNRESOLVED — first-class open reproduction variable.** Treated as a set of labeled sensitivity-analysis configs in Gate 2, never silently resolved to one value and presented as confirmed. |
| Perturbation distribution | Gaussian, i.i.d. per-weight, standard normal scaled by σ | paper §4 | Confirmed |
| Precision | bfloat16 (upstream CLI default) | `upstream_file:randopt.py` CLI `--precision` default | Confirmed as upstream default; not independently confirmed this is what was used for this specific published run |
| Selection metric | Mean 0/1 reward on the selection set (same reward function used for final correctness, not a proxy metric) | `upstream_file:data_handlers/base.py:DatasetHandler.postprocess_outputs`, `upstream_file:data_handlers/gqa.py:GQAHandler.compute_reward` | Confirmed |
| Top-K selection rule | Sort all N scored candidates by selection-set score, descending; take the top K as a prefix of that sorted list | `upstream_file:randopt.py:main` | Confirmed |
| Voting procedure | Majority vote: for each test example, take the most-common normalized answer among the top-K models' (non-empty) predictions; an example where every model produced an empty/unparseable answer counts as incorrect (not excluded from the denominator) | `upstream_file:randopt.py:run_ensemble_evaluation` | Confirmed |
| Tie-breaking rule | Not an explicit rule upstream — it is a consequence of Python's `collections.Counter.most_common`, which preserves first-insertion order among tied counts; since models are inserted in selection-score-descending order, ties are implicitly resolved in favor of the higher-selection-score model | `upstream_file:randopt.py:run_ensemble_evaluation` | Confirmed (as an implementation consequence, not a documented design choice) |
| Images reaching the model | **CONFIRMED root cause of the Gate 1 hard fail (baseline 17.94%/15% vs published 56.6%).** No code path in the released repo, at any git revision including the March 2026 paper-era commit, ever constructs `multi_modal_data` for vLLM — `randopt.py` formats GQA prompts via `tokenizer.apply_chat_template` into plain text (verified locally: inserts `<|vision_start|><|image_pad|><|vision_end|>` placeholder tokens only) and calls `generate()` with bare strings, so the model answers blind. The earlier "missing `multimodal=True` flag" question is subsumed by this — the flag alone would not have helped. | GPU-confirmed, 100-example paired audit, seed=42 (`GATE1_DIAGNOSIS.md`): text-only 15.0%/22.0% (head/march scoring) vs identical-prompt-with-image 59.0%/59.0%; 45 wrong→correct flips vs 1 reverse; 20/20 question↔image pairs independently verified against the source HF dataset | **Confirmed** |
| Baseline fix (image-aware adapter) | `src/neural_thickets_repro/vlm_adapter.py:generate_with_images` — same data/prompt-text/decoding/scoring as upstream, only change is attaching the image via `multi_modal_data`. Used by `eval_base_image_aware.py` for the Gate 1 baseline only; does not touch perturbation/selection/voting code and cannot start RandOpt. | `src/neural_thickets_repro/eval_base_image_aware.py` | Documented deviation, not an upstream edit |
| Gate 2 restoration semantics (RESOLVED — two supported modes, not one bugfix) | GPU A/B diagnostic (`diagnostics/gate2_restoration_ab.py`, 10 repeated same-seed cycles, seed=999999999, sigma=0.01, 5 fixed examples) is conclusive: `perturb_self_weights`/`restore_self_weights` ("released_compat") leaves real, roughly-stable-not-growing residual drift (max abs parameter drift 3.125e-02; raw-output restore 1/5, extracted-answer restore 3/5, correctness agreement 5/5; visual frozen) — consistent with the "Perturbation formula" row above (native-dtype bf16 reseed-and-subtract, not a stored-copy restore). `apply_perturbation`/`reset_to_base_weights` ("fixed_base") showed exactly zero measured parameter drift and exact 5/5 restore on both raw output and extracted answer (visual frozen). Both are real, unmodified upstream `WorkerExtension` mechanisms — this is not "released_compat has a bug fixed_base fixes," it's two different scientific interpretations of "perturb from theta" (delta from current, possibly-drifted state vs. independent draw from the exact stored base), so `run_randopt_image_aware.py` supports BOTH explicitly via a required (never-defaulted) `--restoration-mode released_compat\|fixed_base` flag: `released_compat` for paper-code reproduction / Gate 2 mechanics fidelity, `fixed_base` for WACV-extension experiments where independent-per-candidate sampling is the actual scientific claim. Recorded in the candidate ledger (`CandidateRecord.restoration_mode`), `run_metadata.json`, and `results.json` for every run. | `upstream_file:utils/worker_extn.py:WorkerExtension.perturb_self_weights`/`restore_self_weights`/`apply_perturbation`/`reset_to_base_weights`; `src/neural_thickets_repro/diagnostics/gate2_restoration_ab.py`; `src/neural_thickets_repro/run_randopt_image_aware.py` (`_perturb_call`/`_restore_call`, `RESTORATION_MODES`) | **Resolved: preserve both modes, require explicit selection.** N=20 may now proceed under an explicitly chosen `--restoration-mode` |
| Gate 2 fix (image-aware RandOpt adapter) | `run_randopt_image_aware.py` — the same one deviation (image-aware prompts via `vlm_adapter.build_image_aware_requests`, factored out of `generate_with_images`) applied to the candidate-sampling and ensemble loops, since the released code's image-blindness applies identically there. Everything else is reused unmodified from `external/RandOpt`: `core/engine.py:launch_engines`/`cleanup_engines`, `utils/worker_extn.py:WorkerExtension.perturb_self_weights`/`restore_self_weights` (via Ray `collective_rpc`, the actual weight-perturbation mechanism, not reimplemented), `data_handlers/gqa.py:GQAHandler` (data/scoring). Top-K selection and majority voting use our Gate-0-validated `topk_voting.py` (pure Python, unit-tested against synthetic data, mirrors — not copies — the two upstream rows above). Supersedes `run_randopt.py` (which subprocess-wraps unmodified `randopt.py` and would inherit the identical image-blindness) for actual Gate 2 use; `run_randopt.py` is kept only as a reference for "what the literal released code does if run as-is." | `src/neural_thickets_repro/run_randopt_image_aware.py` | Documented deviation, not an upstream edit; prepared, not yet executed (no GPU on this machine) |
| Scoped RandOpt (WACV) — WorkerExtension has no scope mechanism | Investigated the pinned `utils/worker_extn.py`/`core/engine.py` (commit `536df0a308f3990b6270c991fbb96bd0b779a58e`) directly: `store_base_weights()` (called automatically by `launch_engines()`) clones ALL current parameters unconditionally; `apply_perturbation(seed, sigma)` restores from that stored base then applies `_should_perturb`-gated noise; `reset_to_base_weights()` copies the entire stored base back. **None of these methods, nor any other `WorkerExtension` method, takes a scope/component argument** — the upstream mechanism can only express "LM-only" (default) or "everything" (`PERTURB_VISUAL=1`), never vision-encoder-only/merger-only/per-third-LM. Scoped perturbation is therefore a local, package-side extension (`scoped_perturbation.py`), never a modification to `external/RandOpt` — see `SCOPED_PERTURBATION_DESIGN.md`. | `upstream_file:utils/worker_extn.py:WorkerExtension`, `upstream_file:core/engine.py:launch_engines`; `src/neural_thickets_repro/scoped_perturbation.py` | Confirmed — the extension point, not a gap in our understanding |
| Scoped RandOpt (WACV) — runtime parameter namespace | **Correction to the HF checkpoint's flat `model.*` naming**: the loaded/vLLM-wrapped runtime module actually nests the LM under `language_model.model.*` (`language_model.model.embed_tokens.*`/`language_model.model.layers.{0-35}.*`/`language_model.model.norm.*`), with a separate `language_model.lm_head.*` entry even though the checkpoint ties the weight; `visual.*` stays unnested in either form. Since scope selection operates on the real runtime `named_parameters()`, not the checkpoint's safetensors index, `scopes.py` *discovers* which of two explicitly recognized naming conventions (`runtime_wrapped`/`flat_checkpoint`) is present at call time rather than assuming either — hard-fails if ambiguous or if the discovered LM layer indices aren't a complete contiguous range. Both conventions produce the identical 0–11/12–23/24–35 three-way partition at 36 layers. | `src/neural_thickets_repro/scopes.py` (`LM_NAMESPACE_CONVENTIONS`, `detect_lm_namespace_convention`); `SCOPED_PERTURBATION_DESIGN.md` | Confirmed — real runtime logs, not the checkpoint's saved-tensor naming |
| Scoped RandOpt (WACV) — relative-L2 scaling & noise semantics | `sigma_m = r · ‖θ_m‖₂ / √d_m` for scope `m` (`d_m` = deduplicated selected element count, `‖θ_m‖₂` = L2 norm of the deduplicated selected base weights), giving `E[‖Δ_m‖₂] ≈ r·‖θ_m‖₂`. **Noise generation is unchanged from upstream** — each selected tensor still gets a fresh `torch.Generator().manual_seed(seed)` reseeded from the same candidate seed (`perturb_cpu._generate_noise`'s exact convention), not a continuous stream across the concatenated scope and not an independent-Gaussian-per-tensor mode; recorded explicitly per candidate as `noise_semantics="upstream_per_tensor_reseed"`. Tied/shared parameters (e.g. a possibly-tied `lm_head`) are deduplicated by storage (`data_ptr()`), not just by name, guaranteed by `build_scope_manifest` itself regardless of caller input. | `src/neural_thickets_repro/scopes.py:compute_relative_l2_sigma`/`build_scope_manifest`; `src/neural_thickets_repro/scoped_perturbation.py` | Confirmed design, not yet GPU-validated beyond the one authorized mechanical check (`diagnostics/scope_isolation_gpu_check.py`) |
| Scoped RandOpt (WACV) — GPU scope-isolation validation: PASS | `diagnostics/scope_isolation_gpu_check.py` passed on the real 580-unique-tensor runtime model: `vision_encoder` 321 selected / 259 complement, only selected changed, exact reset; `lm_middle` 84 selected / 496 complement (the LITERAL storage complement across the whole model, not an enumerated "vision+merger+other thirds" list, which would have silently undercounted by the 2 non-layer LM tensors outside all three thirds — fixed and covered by a CPU regression test proving `in_scope ∪ out_of_scope == full_vlm`, intersection empty), only selected changed, exact reset; all seven scopes detected correctly. Infrastructure milestone closed — no further scope-diagnostic work added. | `src/neural_thickets_repro/diagnostics/scope_isolation_gpu_check.py` | **GPU-confirmed PASS** |
| Scoped RandOpt (WACV) — thicket measurement (base score, expert density, aggregation) | First scientific-instrumentation milestone (measurement only, no sweep executed): `rho_{m,t}(r) = (1/N) sum_i 1[S(theta+Delta_i) > S(theta)]` (expert density) plus full score-distribution stats (mean/std/quantiles/deltas, 95% Wilson CI) computed against an EXPLICITLY evaluated `base_score` — the exact unperturbed model on the same selection subset candidates are scored against, restored to the exact stored base first (`compute_base_score`), never inferred from a historical baseline (e.g. Gate 1's number, which used a different subset/prompt path). Written per run to `thicket_metrics.json`; Top-K/ensemble machinery preserved for compatibility but not the primary result. `--relative-l2` (radius `r`) is a fixed experimental axis per invocation — no grid, no auto-tuning. `candidate_sampling.sample_candidate_seeds` already guarantees the identical ordered seed sequence across all seven scopes for a fixed (seed, N, r) by construction (scope isn't an input to seed sampling) — proven by test across all seven, not just assumed. `analysis/aggregate_coarse_thicket.py` builds a cross-scope comparison table from completed runs' `thicket_metrics.json` files, hard-failing on any mismatched task/model-revision/GQA-subset/N/seed/radius/scoring-protocol/candidate-seed-sequence. | `src/neural_thickets_repro/thicket_metrics.py`, `run_scoped_randopt.py` (`compute_base_score`), `coarse_thicket_aggregation.py`; `analysis/aggregate_coarse_thicket.py` | Implemented and CPU-tested; **not yet GPU-validated** — awaiting the smallest measurement-path validation command before any radius/N is chosen |
| Scoped RandOpt (WACV) — vLLM multimodal encoder-output cache bug, worker-only reset CONFIRMED INSUFFICIENT | Coarse-map sweep found `vision_encoder r=.005`/`r=.02` producing 0 experts / 100 ties / 0 regressions — every one of 100 independently-perturbed candidates scored exactly the base score (`0.5600`). Root cause: vLLM's per-worker multimodal encoder-OUTPUT cache (distinct from `mm_processor_cache_gb`, the image *preprocessing* cache) serves a cached vision embedding for a repeated image regardless of whether model weights changed since it was computed. The first fix attempt (`vlm_adapter.reset_vllm_encoder_cache`, `collective_rpc("reset_encoder_cache")`, worker-side only) was **confirmed insufficient on GPU**: `RuntimeError: Encoder cache miss for <mm_hash>`, with scheduler output immediately before the crash showing `scheduled_encoder_inputs={}` — proof the driver-process scheduler's own bookkeeping still believed the (now worker-cleared) embedding was cached and never scheduled recomputation, exactly the predicted limitation from the prior investigation. | see next row for the fix | **Confirmed insufficient — superseded, see next row** |
| Scoped RandOpt (WACV) — full engine-level encoder-cache reset (scheduler + worker) | Fix: `vllm.entrypoints.llm.LLM` has no `reset_encoder_cache()` method (confirmed, both attempts); `LLMEngine.reset_encoder_cache()` exists and clears BOTH the scheduler's bookkeeping and the worker-side cache via `self.engine_core.reset_encoder_cache()`, but `LLM` never forwards to it, and Ray's actor RPC only exposes methods that exist on the wrapped class — so it's normally unreachable from outside the Ray actor. Resolved by reading `external/RandOpt/core/engine.py:launch_engines` directly: it applies `ray.remote(...)` to `RandOptNcclLLM` **fresh, inside the function body, on every call** (not a class-level `@ray.remote` decorator) — so `vlm_adapter.ensure_full_encoder_cache_reset_exposed()` adds a `reset_encoder_cache_full` method to the already-imported `RandOptNcclLLM` class object (a runtime attribute addition to an in-memory Python object, in our own package's code — `external/RandOpt` is never edited on disk, no subclass, `launch_engines()` itself unmodified) BEFORE `launch_engines()` is called, so Ray's actor-method registry includes it. `vlm_adapter.reset_vllm_encoder_cache_full()` then calls it as a genuine Ray actor method (`engine.reset_encoder_cache_full.remote()`, like `engine.generate.remote(...)`, NOT `collective_rpc`), reaching `self.llm_engine.reset_encoder_cache()` on the real instance — the full scheduler+worker reset. Wired into `run_scoped_randopt.py`'s sampling/ensemble phases for `vision_encoder`/`vision_merger`/`full_vlm` only, superseding the worker-only reset for the scientific path (kept in `vlm_adapter.py` for reference, no longer called from the candidate loop). Hard-fails if the actor doesn't expose the method or the call errors. | `upstream: vllm/entrypoints/llm.py`, `vllm/v1/engine/llm_engine.py`, `vllm/v1/core/sched/scheduler.py:2487`, `vllm/v1/executor/abstract.py:314`, `vllm/v1/worker/gpu_worker.py:858` (pinned tag `v0.27.1`, commit `6e448d0ea9bf3d88d898b65449ca6dc2aec170ac`); `external/RandOpt/core/engine.py:launch_engines`/`RandOptNcclLLM` (pinned commit `536df0a308f3990b6270c991fbb96bd0b779a58e`, read not edited); `src/neural_thickets_repro/vlm_adapter.py:ensure_full_encoder_cache_reset_exposed`/`reset_vllm_encoder_cache_full` | **Fix implemented and CPU-tested; awaiting GPU N=2 validation before resuming the sweep.** `full_lm` (all 4 radii) preserved unchanged, not rerun; `vision_encoder r=.005`'s pre-fix artifact AND the worker-only-reset crash log preserved as forensic records, excluded from the final coarse map |
| Scoped RandOpt (WACV) — vision-encoder sub-scopes (`vision_early`/`vision_middle`/`vision_late`) | Motivated by the completed, validated 7×4 coarse map showing `vision_encoder`'s highest expert density at `r=.04` (0.87) / `r=.07` (0.89): a fixed, non-uniform contiguous partition of the 32 vision-encoder blocks (32 is not divisible by 3, so `partition_layers_into_thirds`'s equal-thirds rule doesn't apply) — `vision_early` = `visual.patch_embed.*` + blocks 0–10 (+ `visual.rotary_pos_emb.*` if any trainable parameters exist there), `vision_middle` = blocks 11–21, `vision_late` = blocks 22–31. `visual.merger.*` stays excluded from all three; union of the three exactly equals `vision_encoder`'s own selection, zero overlap (proven by test, not just inspected). `scopes.discover_vision_block_indices` hard-fails unless the complete `{0,...,31}` block set is found — no convention-discovery step needed (unlike LM layers), since `visual.*` is confirmed never re-nested under `language_model./model.` in either LM convention. All three added to `_VISUAL_AFFECTING_SCOPES` (encoder-cache reset required). Relative-L2 sigma derived independently per sub-scope from its own manifest via the same unmodified `compute_relative_l2_sigma` — `scoped_perturbation.py` required no changes, being fully scope-agnostic already. Scientific protocol otherwise unchanged (same model/GQA/subset/scorer/`fixed_base`/`upstream_per_tensor_reseed`/N=100/K=1/test_samples=5); only `r ∈ {0.04, 0.07}` used, no new radii. 3 scopes × 2 radii × 100 candidates = 600 candidates, **not run by this milestone**. | `src/neural_thickets_repro/scopes.py` (`discover_vision_block_indices`, `partition_vision_blocks`, `_make_vision_third_selector_factory`); `SCOPED_PERTURBATION_DESIGN.md` "Vision-encoder sub-scopes" addendum | Implemented and CPU-tested (294 passed, 1 skipped); GPU scope-isolation Tests C/D/E prepared (`diagnostics/scope_isolation_gpu_check.py`) but not yet run — real per-scope tensor/element counts and L2 norms are not fabricated here, only obtainable from that GPU command |
| Scoped RandOpt (WACV) — vision_late sub-scopes (`vision_late_a`/`vision_late_b`) | Motivated by the 6-cell vision-localization sweep's paired seed-level analysis (all six cells share the same 100 candidate seeds): at `r=.04`, `vision_late` showed significantly higher expert density than `vision_early` (exact McNemar p=.0227) and `vision_middle` (p=.0192, paired mean delta −.0065, 95% bootstrap CI [−.0118,−.0012]); no pairwise comparison was significant at `r=.07`. Fixed 5/5 split of `vision_late`'s own block range: `vision_late_a` = blocks 22–26, `vision_late_b` = blocks 27–31. Union exactly equals `vision_late`, pairwise disjoint, no merger/out-of-range parameters (proven by test). Both added to `_VISUAL_AFFECTING_SCOPES`. Relative-L2 sigma derived independently per half from its own manifest via the unmodified `compute_relative_l2_sigma` — `scoped_perturbation.py`/`run_scoped_randopt.py` CLI needed zero changes, both already fully scope-agnostic. Only `r=0.04` used, no `r=.07`. 2 scopes × 1 radius × 100 candidates = 200 candidates, **not run by this milestone**. | `src/neural_thickets_repro/scopes.py` (`partition_vision_late_into_halves`, `_make_vision_late_half_selector_factory`); `SCOPED_PERTURBATION_DESIGN.md` "vision_late sub-scopes" addendum | Implemented and CPU-tested (312 passed, 1 skipped, zero regressions); GPU scope-isolation Tests F/G prepared (`diagnostics/scope_isolation_gpu_check.py`) but not yet run |
| Weight-reconstruction script model class | Upstream's standalone single-expert reconstruction script loads the checkpoint via a causal-LM-only model class, which is not the correct class for this VLM checkpoint (`Qwen2_5_VLForConditionalGeneration`) | `upstream_file:utils/repro_seed.py` vs. checkpoint `config.json` `architectures` field | **Confirmed discrepancy** — documented and corrected only in our own `src/neural_thickets_repro/vlm_adapter.py`, never by editing the external clone |

## Sigma — resolution plan (see Pipeline gates in the plan)

Gate 2 (small-scale RandOpt on real GPU hardware) will run each candidate sigma set below as a labeled,
independent sensitivity configuration and record all results side by side. No candidate is presented as
"the" reproduced sigma unless the paper's ambiguity is independently resolved first (e.g. by re-reading
the full paper with more budget, or by locating an author clarification). The candidates, as of this
writing:

1. `sigma_default = [0.0001, 0.0005, 0.001, 0.002, 0.005, 0.01]` — upstream CLI default.
2. `sigma_example_scripts = [0.0005, 0.001, 0.002]` — upstream `scripts/local_run.sh` / `scripts/single_node.sh`, used there for Olmo-3-7B/countdown, not confirmed for Qwen-VL/GQA.
3. `sigma_appendix_e3 = [0.005]` — paper Appendix E.3's single stated value, ambiguous scope.

`configs/gqa_repro.yaml` leaves `randopt.sigmas` as `null` with a comment pointing here.

## Explicitly out of scope for this phase

Layer localization, module-wise RandOpt, construction datasets, low-rank search, CoRP, Iterative RandOpt,
routing, distillation, continual learning, and any other WACV-extension work. This document and the
accompanying Gate 0 scaffold cover reproduction of the published §5.2 result only.
